# Template for /etc/backstage/backstage.env (the real file lives OUTSIDE Git).
#   sudo install -m 0640 -o root -g docker deploy/backstage.env.example /etc/backstage/backstage.env
BACKSTAGE_BASE_URL=https://backstage.example.com
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_USER=backstage
POSTGRES_PASSWORD=
POSTGRES_DB=backstage
AUTH_GITHUB_CLIENT_ID=
AUTH_GITHUB_CLIENT_SECRET=
# GITHUB_TOKEN=
