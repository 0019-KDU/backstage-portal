[Unit]
Description=Backstage PostgreSQL logical backup (pg_dump)
After=postgresql.service
Requires=postgresql.service

[Service]
Type=oneshot
User=postgres
Group=postgres
Environment=POSTGRES_DB=backstage BACKUP_DIR=/var/backups/backstage
ExecStart=/usr/local/sbin/backstage-backup-postgres
Nice=10
